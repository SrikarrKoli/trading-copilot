NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_your_key
NEXT_PUBLIC_SITE_URL=http://localhost:3000
AUTH_OWNER_EMAIL=owner@example.com
LOCAL_AUTH_BYPASS=false

# Schwab credentials remain intentionally absent until the approved
# application provides its current OAuth contract. Future secrets must stay
# server-only and must never use a NEXT_PUBLIC_ prefix.
